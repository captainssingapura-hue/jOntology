package hue.captains.singapura.tao.ontology;

/**
 * A functional object contains a set of functions.
 *  The main usage is to group functions into set for better manageability and comprehensibility.
 *   It could be stateless or with immutable states.
 */
public interface FunctionalObject extends Immutable{
}
